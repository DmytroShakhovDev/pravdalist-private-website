/**
 * app.js — Vue 3 island bootstrap
 * Mounts Vue apps for interactive sections.
 */
const { createApp, ref } = Vue;

// Language switcher island is mounted in lang-switcher.js
// Tiles island can be extended here if needed
